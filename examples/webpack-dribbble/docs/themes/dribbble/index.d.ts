import React from 'react';
declare type Props = {
    children?: React.ReactNode;
};
declare const ThemeScope: {
    (props: Props): JSX.Element;
    lightClassName: any;
    darkClassName: any;
};
export default ThemeScope;
